# Plain-text password for the 'arko' login — no escaping needed
APP_PASSWORD=your_password_here

# Random 32+ character string — keep secret
JWT_SECRET=<random 32+ char string>

# Optional: enables Gemini Flash fallback for image parsing
GEMINI_API_KEY=

# Data directory (SQLite DB + uploaded images)
DATA_DIR=/data
