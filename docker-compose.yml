version: "3.9"

services:
  runner:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - runner_data:/data
    env_file:
      - .env
    environment:
      - DATA_DIR=/data

volumes:
  runner_data:
